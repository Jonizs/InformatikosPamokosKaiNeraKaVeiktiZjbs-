# assets/data/

`usage.json` goes here — generated by `tools/export-usage.py` from your local
Claude Code transcripts. It is gitignored: it is your data and this site is
public.

The dashboard fetches `assets/data/usage.json` on load. If the file is absent
it falls back to the generated demo dataset and says so in a banner on every
view.
